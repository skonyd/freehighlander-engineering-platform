# Claude entry point

Read and follow `AGENTS.md` first.

For a resume request such as **"3'lü modda devam edelim"**:
1. read `.freehighlander/state.yaml`,
2. read `PROJECT_STATE.md`,
3. follow `docs/state/RESUME-PROTOCOL.md`,
4. use `docs/state/TRIPLE-MODE.md` for the three-model working protocol.

Do not rely on previous Claude chat history as the source of truth.
