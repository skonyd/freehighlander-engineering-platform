# Project Checkpoint History

Repository-native checkpoint entries are appended by:

```bash
npm run project:checkpoint -- --write
```

A checkpoint records handoff state only. It never implies that a semantic gate passed.
